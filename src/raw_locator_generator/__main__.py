"""
Main entry point for running the raw_locator_generator package as a module.

This allows running the package with: python -m raw_locator_generator
"""

from raw_locator_generator.dom_extractor_agent import main

if __name__ == "__main__":
    main()
